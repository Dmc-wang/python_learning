第一步：理解骨架
    把下载文件夹里的“垃圾”自动分类到子文件夹
    输入：--src C:\Users\Download
    输出：自动创建pdf\ images\ code\等子文件夹，并把文件移进去
    核心设计模式：CLI工具 = 参数解析 + 业务逻辑 + 异常处理

第二步：搭建最小可运行版本
    先让脚本能跑起来，打印“hello world”

第三步：实现文件扫描
    能列出文件夹里所有文件

第四步：实现分类逻辑
    根据扩展名，把文件分到对应类别

第五步：实现安全移动
    把文件真正移动到分类文件夹，但不能覆盖

第六步：加入CLI参数
    让 --dry-run 和 --recursive 生效。

第七步：加入日志系统
    用 logging 替代 print。

第八步：最终完善
    加上异常处理、性能计时、扩展名过滤等工程特性。

上面是基本完成，接下来是补上几个关键功能
第一步：让 --dry-run 真正生效
    safe_move_file 里没有判断 dry_run

第二步：修复 --recursive 递归功能
    organize_folder 里直接用 src.iterdir()，不支持递归

第三步：添加文件统计功能
问题：organize_folder 没有返回统计结果

第四步：最终实现完整版
把 main 函数里被注释的代码激活，并加入计时：